# Invariants

**Status:** normative, and shipping. Almost every mechanism below is `landed` — the named test or
gate exists and runs in `make check` or a CI job. The **Lands in** column says when for the few that
are not.

A rule without a mechanism is a wish. Every rule on this page names the thing that enforces it — a
database trigger, a lint rule, a test, or a CI gate — because a rule enforced only by review is a
rule that survives until the first tired Friday.

**Some rows say the mechanism is missing, and they start `NOT HELD`.** They are here rather than
deleted because the rule is still real and somebody has to be able to find the gap; a rule quietly
dropped from this page is indistinguishable from one nobody ever wanted. A row that names a test,
a script or a trigger is a claim that it exists — an audit at `09d1e9f` found three that did
not, and they are now written the other way round.

## Data invariants

| Invariant | Enforced by | Lands in |
|---|---|---|
| `tod_report`, `quake_event`, `invite_redemption`, `identity_link`, `instance_grant`, `audit_log` and `event_outbox` are never updated or deleted | `BEFORE UPDATE OR DELETE … RAISE(ABORT)` triggers, **plus** `TestAppendOnly_TriggersFire_AfterAllMigrations` — table rebuilds drop triggers, and that test is how you find out. It asserts the trigger **aborts**, never that it appears in `sqlite_master`. `LOG001` additionally fails an `UPDATE` or `DELETE` against one of these tables anywhere in `db/queries` or `db/migrations-sqlite`, catching the statement before it ships | landed |
| A report may be retracted at most once | The partial unique index `ux_tod_report_retracts` | landed |
| Every table is `STRICT` | Atlas generates the DDL from `db/schema.hcl`; `TestSchema_EveryTable_IsStrict` reads `PRAGMA table_list` back off a migrated database, and `TestIntegrityCheck_MigratedDatabase_Passes` runs `PRAGMA integrity_check` and `PRAGMA foreign_key_check` | landed |
| Every circle-scoped table carries `circle_id NOT NULL REFERENCES circle(id)` | `TestInstanceScopedAllowlist_MatchesTheAppliedSchema`, enumerating the applied schema against the allowlist parsed out of [canonical §9](../design/00-canonical-conventions.md#9-tenancy--this-project-diverges-from-dkp). `circle` itself is the one named exception: its own `id` is the tenant key | landed |
| Every circle-scoped query names `circle_id` in its `WHERE` | `TEN001` over `db/queries/*.sql`, same allowlist, **per query rather than per file** — and blind to a `circle_id` that appears only in a comment or an `ORDER BY`. A query that legitimately spans circles carries a counted `-- tenancy:` waiver. `test/repo` asserts the gate fires on each of those cases | landed |
| The instance-scoped allowlist in [canonical §9](../design/00-canonical-conventions.md#9-tenancy--this-project-diverges-from-dkp) and `INSTANCE_SCOPED` in `scripts/repo-gates.sh` are the same list | `TestInstanceScopedAllowlist_MatchesRepoGates`, which parses both and compares them in **each** direction, plus `TestInstanceScopedAllowlist_MatchesTheDomainModel` for the third copy in the domain model's own table. Two hand-maintained copies of one fact is exactly the drift this repository gates against everywhere else, and the copy that silently grows is the one that stops a table being tenancy-checked | landed |
| An identity holds an instance permission because of a DECISION somebody made, and the decision to take it away is as durable as the one that gave it | `instance_grant` is append-only by trigger — `TestAppendOnly_TriggersFire_AfterAllMigrations` covers it with every other append-only table — so a revocation is a NEW ROW naming the grant it supersedes rather than a `DELETE`. It is hash-chained for the same reason `audit_log` is, and it has to be its own audit record because `audit_log.circle_id` is `NOT NULL` and an instance grant belongs to no circle. [ADR-0012](../adr/0012-instance-grants-are-a-capability-ledger.md) | landed |
| The ledger can always be appended to, whichever generator minted the last row | The hash chain's tail is the row whose `hash` no other row names as its `prev_hash` — derived from the CHAIN, never `ORDER BY id`. A ULID is monotonic within one generator and `tod-serve instance grant` builds a fresh one per invocation, so two invocations inside one millisecond mint from random entropy and the later row can sort below the earlier one; an id-ordered head then returned the earlier row forever, the next append reused a `prev_hash` already claimed, and `ux_instance_grant_chain` refused it — permanently. `TestDecide_TwoGeneratorsInOneMillisecond_CanStillAppend` forces the ids into the wrong order rather than racing for it. `ux_instance_grant_hash` makes the derivation total: two rows sharing a hash would give the ledger two tails or none | landed |
| Exactly one decision is current for one identity and one permission | Two partial unique indexes, not a sort: `ux_instance_grant_head` permits one chain root per pair and `ux_instance_grant_supersedes` permits one row to supersede one row, so the tail is unique **by construction** and `Service.Effective` needs no `ORDER BY` and no tie-break. `ux_instance_grant_chain` refuses two rows naming the same `prev_hash`, so a forked chain — one that verification would follow down one branch and never see the other — is unrepresentable too. `TestInstanceGrant_AForkedChain_IsRefusedByTheSchema` drives all three | landed |
| `instance_grant.permission` holds instance-realm keys and nothing else | A `CHECK` GENERATED from `internal/authz` into `db/enums.hcl` by the same path every other enumerated column takes, so the list cannot drift from the catalogue. `TestInstanceGrantPermissions_TheGeneratedCheck_IsTheInstanceRealm` compares the two in both directions, and `TestInstanceGrant_ACircleRealmPermission_IsRefusedByTheSchema` walks the whole catalogue against the applied database — without that second half the Go refusal in `internal/instancegrant` would be one mechanism wearing two hats | landed |
| A `credential_ticket` is redeemable **once**, at either `/join` or `/sessions` | `ConsumeCredentialTicket` writes `consumed_at` under `WHERE consumed_at IS NULL`, **and** `trg_credential_ticket_single_use` aborts any update to an already-consumed row, so a replay is unrepresentable rather than merely checked. `trg_credential_ticket_facts_are_immutable` stops the guild facts being edited between minting and the gate. `TestCredentialTicket_SecondConsumption_Refused` at the database, `TestCredentialTicket_SecondConsumptionThroughTheAdapter_Refused` for the path the flow actually takes, and `TestCredentialTicket_SecondRedemption_Refused` in `internal/identity` for the `401 auth_ticket_invalid` a client sees | landed |
| A `credential_ticket` expires 120 seconds after it is minted | `CHECK (expires_at = created_at + 120 * 1000000)`, so a longer-lived ticket cannot be written at all — `TestCredentialTicket_LongerThan120s_IsUnrepresentable`. `TestCredentialTicket_AnyOtherTTLThroughTheAdapter_IsUnrepresentable` drives four wrong lifetimes through the adapter. `expires_at` is then checked against the injected clock at redemption; `TestCredentialTicket_After120s_Refused` in `internal/identity` — `401 auth_ticket_expired` | landed |
| An identity with `blocked_at` set cannot join **any** circle on the instance | `TestJoin_BlockedIdentity_Refused`, in `internal/identity` for the service and in `internal/membership` for the join path — the block is checked at ticket redemption *and* against the `identity` row the membership is about to point at, which are two different moments. The membership test blocks an identity and then tries a SECOND circle on the same instance, whose officers have never heard of them. `403 identity_blocked` | landed |
| `identity_provider.client_id` is present exactly when the provider talks to a third party | `CHECK ((kind = 'local') = (client_id IS NULL))` — a `discord` or `oidc` provider row with no operator application is unrepresentable, not merely invalid, and `Provider.Validate` mirrors it for rows assembled without going through the database. It read `((kind = 'discord') = (client_id IS NOT NULL))` until migration `000003`, which made `oidc` unconfigurable: `aud = client_id` **is** the OIDC audience check, so a row that cannot carry a client id cannot verify an audience | landed |
| `circle.server` never changes after creation | `trg_circle_server_is_immutable`, a `BEFORE UPDATE` trigger keyed on the value changing rather than on `UPDATE OF server`; `TestCircleServer_Update_IsRefusedByTheDatabase`. At the edge, `updateCircle` answers `422 field_immutable` for a body that names `server` **at all**, including one naming the value it already has — `TestUpdateCircle_Server_Is422FieldImmutable`. `db/queries/circle.sql` has no query that sets it, which is the third copy of the same rule | landed |
| A revoked identity cannot rejoin the same circle | The partial unique index `ux_membership_identity`, which makes a second membership row unrepresentable — there is no delete-membership query in `db/queries` at all. `TestMembership_SecondRowForOneIdentity_IsUnrepresentable` | landed |
| An `identity_link` participant has `verifiable_subject = 1` | `trg_identity_link_requires_verifiable_participants`, which counts verifiable participants and refuses anything but two — so a missing identity row fails the same test; `TestIdentityLink_LocalProvider_Rejected` | landed |
| `identity_provider.verifiable_subject` matches its `kind` | `CHECK ((kind = 'local') = (verifiable_subject = 0))` — it is not an operator toggle | landed |
| Enum values are identical in the SQL `CHECK`, the JSON and the OpenAPI | One Go catalogue in `internal/schemaenum`, bound to its columns by `internal/dbschema`; `make gen` writes `db/enums.hcl` and Atlas writes that into the migration. `TestEnumColumns_AppliedSchema_MatchTheCatalogue` reads every `CHECK` back out of a migrated database and compares it to the catalogue, and `TestEnumBindings_EveryCatalogueEnum_IsBoundOrExplicitlyUnstored` refuses an enum that is neither stored nor recorded as derived. The OpenAPI half is **narrower than this row reads**: `TestSpec_TheProblemSchema_PublishesTheClosedCodeEnum` and `TestSpec_EveryDeclaredScope_IsInTheCatalogue` cover the error-code and scope enums, and no test compares the rest of the `schemaenum` catalogue against `openapi/openapi.json` | SQL and JSON landed; the OpenAPI half covers two enums of the catalogue |
| The enum catalogue cannot drift from the canonical conventions | `TestAll_Catalogue_MatchesCanonicalConventions` parses the fenced block in [canonical §5](../design/00-canonical-conventions.md#5-enums) and compares element by element | landed |
| The ordered enums rank in the order the canonical conventions state | `TestRank_OrderedEnums_MatchTheCanonicalOrdering` reads the ordering out of the document's own sentence | landed |
| A migration that has shipped in a tagged release is never edited | `db/migrations-sqlite/atlas.sum` is checked in, and `make gen` refuses to run against a directory that does not match it rather than silently re-hashing — so an edit to an applied migration is a diff a reviewer sees. Comparing checksums against the **previous release** needs releases, and `git tag` is still empty — so the second half waits on the first tag rather than on a phase | partly landed; release comparison waits on a tagged release |
| A circle-scoped row never references another circle's row | Every reference from a circle-scoped table to a circle-scoped table is a **composite** foreign key carrying `circle_id` — `(circle_id, reporter_membership_id) REFERENCES membership (circle_id, id)`. A single-column key proves only that the row exists, so without this a report in circle B can name a reporter from circle A with both keys satisfied. `TestSchema_EveryCircleScopedForeignKey_NamesTheCircle` walks the applied schema so the next such column cannot be forgotten; `TestTodReport_ReporterFromAnotherCircle_IsRefused` and `TestTodReport_RetractionAcrossCircles_IsRefused` are the write attempts | landed |
| One spelling belongs to one target | Names and aliases share a namespace. Neither unique index can say so — SQLite has no constraint spanning two tables — so four triggers in `000005_raid_target_name_namespace.sql` do, and `TestRaidTargetNamespace_TriggersFire_AfterAllMigrations` asserts an ABORT rather than a row in `sqlite_master`. Without it an alias can be hung on a different target and the ladder answers that spelling with the canonical-name target, because `name_norm` is rung two and `alias_norm` is rung four: the alias silently resolves to somebody else's mob. `internal/catalogue` checks the same rule inside its write transaction so the API answers `409` naming the collision |
| A moved respawn window is pushed at the projection | `Route.InvalidatesTimer` on every route that writes one. `TestRouteRegistry_EveryTimerWritingRoute_PushesTheInvalidation` drives each and asserts the invalidator fired; `TestRouteRegistry_EveryWindowWritingPath_CarriesTheFlag` closes the other direction so the flag cannot be dropped to silence the first. The other four `change_reason` values are read back off the report log — `timer_change` is the one a moved window leaves no row for, so it is the only one that has to be pushed |
| A flipped `is_quake_target` is pushed at the projection | `raid_target.is_quake_target` is a derivation input and not identity: the resolve ladder copies it onto every `consensus.Timer` it returns, so flipping it decides whether a kill before the latest quake still counts. `updateRaidTarget` moves no window, carries no `Route.InvalidatesTimer` and is therefore invisible to the two gates above — it pushes `catalogue.TimerInvalidator`'s third method instead, inside its own transaction. **The gate is behavioural rather than a fourth registry flag, deliberately:** a flag needs a second, independent derivation or it is one deletion from being vacuous, and the path convention that derives `InvalidatesTimer` (`/timers/`, `/timer-overrides/`) has no counterpart that says "this body carries a derivation input". `TestQuakeTargetFlip_TheBoardAndTheDetailPage_Agree` flips the flag on a quiescent instance and requires the board and `getTargetState` to give one answer; `TestUpdateRaidTarget_OnlyAQuakeFlagFlip_PushesTheInvalidation` drives the route for both directions, because a push on every update would recompute every board on the instance whenever somebody fixed a typo. Issue #21 |
| A route that revalidates says so in the contract | `Route.ConditionalRead` drives the documented `304` and its `ETag` header, because the framework cannot infer one from a dynamic `Status` field and a real response absent from the document is one a generated client treats as an undocumented error. `TestRouteRegistry_ConditionalRead_MatchesWhatTheRouteActuallyDoes` drives every ETag-returning GET twice and requires the flag to predict which answers `304` — **the behaviour is the second derivation on purpose**: the document's 304 is generated FROM the flag, so a spec-only check compares a derivation against itself and passes for a route flagged at random. `getCircle` and `getMember` declare `If-None-Match` and do not revalidate; that gap is pinned by the same test rather than written down somewhere |
| A non-5xx answer from a window-writing route means the projection was told | Held by construction since [ADR-0013](../adr/0013-the-timer-invalidation-joins-the-writing-transaction.md): the push runs inside the write's own transaction, so an answer that reports a moved window is an answer whose boards were recomputed with it. It used to be held by compensation — the push ran before the answer on **every** outcome including the `404` of a delete that found nothing, because a committed DELETE left a failed push nothing to retry from. A rolled-back one does, so the compensation is gone and the `404` now recomputes nothing: nothing moved, and `timer_change` on a board whose window did not move is a small lie in the one field that exists to explain why an answer changed. `TestTimerWritingRoutes_EveryTerminalOutcome_LeavesTheProjectionCurrent` walks create, delete and delete-again and pins the push count of each; `TestDeleteCircleTimerOverride_AFailedInvalidation_LeavesTheOverrideAndConverges` asserts the row is still there for the retry |
| A resolved timer follows one precedence | Circle override, then catalogue, then `unknown`. `catalogue.Service.ResolveTimer` and `ResolveTimers` share `resolveTimerFrom`, so the single-target and whole-board paths cannot drift; `TestResolveTimers_TheBulkPath_AgreesWithTheSingleOne` asserts it |
| A raid target is matched on `name_norm` and never on a collation | `core.Normalise` in Go, one resolve ladder in `internal/catalogue`, and the board's `q` runs its substring rung rather than a second matcher. `TestResolve_TheLadder_RanksAnExactHitAboveASubstringOne` drives the rungs against inputs that would each match a lower one |
| A timer window is never half-specified | Four `CHECK`s per timer table, each written so it **cannot evaluate to NULL** — SQLite treats a NULL `CHECK` as satisfied, so the obvious spelling of the domain model's three rules accepts a fixed timer with a NULL close offset, an `unknown` timer that kept a close offset, and any row whose ordering comparison went NULL. `TestTimerWindow_MalformedRows_AreRefused` drives eleven shapes through both `raid_target_timer` and `circle_timer_override` | landed |
| Migrations are forward-only | Every `Down` block contains `RAISE(ABORT, …)`; `MIG001` fails any migration whose `Down` contains DDL, or whose name is not `NNNNNN_snake_case.sql`, or which is out of sequence. `test/repo` asserts it fires on each | landed |
| `db/queries/*.sql` contains no non-ASCII character | `SQLC001`. Not a style rule: sqlc rewrites `sqlc.arg()` by **byte** offset while reporting positions in **runes**, so one em dash in a comment silently corrupts every query after it in the file — the Go still compiles and the SQL is mangled. Found once; gated so it is not found twice | landed |
| The migration directory replays to exactly `db/schema.hcl` | `make gen` re-runs `atlas migrate diff` after rewriting what Atlas wrote, and fails if anything is still pending. That is what makes the backtick and `NULL` rewriting in `scripts/gen-schema.sh` safe to trust | landed |

## Consensus invariants

| Invariant | Means | Enforced by |
|---|---|---|
| `Pure` | `internal/consensus` imports no store, no `time.Now`, no `math/rand` | `PURE001`, `PURE002`, `CLOCK001`. `PURE002` is an import deny-list rather than a call grep, so a package that has no way to *spell* a clock cannot grow one |
| `OneDerivation` | `internal/projection` re-renders a cached estimate's status and window through `consensus.Project` rather than through a copy of §6 and §7 | `TestProject_OverTheGoldenCorpus_AgreesWithDerive` replays every fixture and requires Project's answer to equal Derive's. Both call the same two unexported helpers, which is why Project lives in `internal/consensus` rather than in the package that needs it |
| `NoFloat` | No `float32`/`float64` anywhere in the window computation, because a boundary computed in float is not bit-identical across platforms and the nightly verify job diffs exact values | `NOFLOAT001` is the grep pre-check; `TestNoFloat_Package_ContainsNoFloatingPointAtAll` parses the package and is the authority, because `x := 1.5` is a float that never spells the word. [Canonical §3](../design/00-canonical-conventions.md#3-no-floats-anywhere-that-computes-a-window) also names a `golangci-lint` rule: `.golangci.yml` records that one cannot be written without switching the other `forbidigo` rules off everywhere else, so the parser carries that half |
| `Deterministic` | The same inputs produce byte-identical output on every platform | The golden corpus, run on every CI platform, plus `TestDerive_ReportsInAnyOrder_SameState` and `TestDerive_CalledTwice_ReturnsEqualStates` — map iteration order is how a Go derivation usually stops being one |
| `ClusterSpanBounded` | No cluster spans more than 2ε, so reports cannot chain into a fictitious multi-hour kill | `TestCluster_GeneratedSequences_SpanNeverExceedsTwoEpsilon` over a seeded generator — `math/rand` is banned, so the seed is a written-down constant and a red build reproduces — and `TestCluster_AdversarialSequences_SpanNeverExceedsTwoEpsilon` over the boundaries a generator hits rarely |
| `LatestDiedAtWins` | The current cluster is the one with the latest `died_at`, never the most recently reported | Golden fixture `backfilled_older_report.json`, plus `TestDerive_ReportedAtPermuted_SameState` over the whole corpus: system truth may say anything at all and the answer does not move |
| `ObservationNeverVetoed` | A physically implausible `died_at` is flagged, never rejected — derived state must not veto an observation | Golden fixture `implausible_ordering.json` |
| `RevokedReportsCount` | A revoked member's reports still contribute, and their retractions still apply | Golden fixtures `revoked_reporter.json` and `revoked_reporter_retraction.json`, the second of which must produce the same state as `retraction_folding.json` |
| `CacheIsNotAuthority` | A nightly job recomputes every state from the reports and diffs; the recomputation wins and an alert fires | `projection.Service.Verify`, reached by `tod-serve verify-states`, which exits non-zero when it repaired anything so a scheduler notices. `TestVerify_ACorruptedCacheRow_IsRepairedAndAlerted` corrupts a row over real SQLite and asserts all three halves — the recomputation won, the discrepancy was named field by field, and an ERROR was logged. `TestVerify_AHealthyInstance_IsSilent` is the other half: a job that alerted every night is a job somebody turns off |
| `ConditionalReadIsDeclared` | Every ETag-returning GET answers `304` to a caller holding its tag, in ONE shape, and the document says so | `withConditionalGet` makes the behaviour total rather than per-handler, so the pairing is asserted from both ends: `TestRouteRegistry_ConditionalRead_MatchesWhatTheRouteActuallyDoes` drives every such route twice and requires the flag to predict the second answer, and `TestRouteRegistry_EveryETagReturningGET_DeclaresItsConditionalRead` catches the row a driver does not reach. Comparing the flag against the document alone would compare a derivation with itself. The behavioural gate also asserts the SHAPE — no `Content-Type`, no `Content-Length`, the `ETag` repeated, per RFC 9110 §15.4.5 — because there were briefly two ways to produce a `304` and only one of them was right |
| `NoCircleEnumeration` | The two queries that return circles without naming one are unreachable from any caller-facing path | `TestCircleEnumeration_IsReachableOnlyFromTheProjection` scans every Go file and confines `ListLiveCircles` and `ListLiveCirclesOnServer` to `internal/projection`, `cmd/` and `internal/setup`. The last is the one caller-facing exception and is admitted on a narrower argument than "an operator ran it": the enumeration reaches a caller only through a route carrying `Auth: AuthSetupToken`, which answers `404` without `TOD_SETUP_TOKEN` and `409` once anybody administers the instance — so its reader could grant themselves ownership of every circle it lists ([ADR-0016](../adr/0016-first-run-setup-is-an-env-token-and-a-derived-window.md)). Both carry a counted `-- tenancy:` waiver whose justification is what makes them SAFE — no caller-supplied circle, and no result that reaches a response body — rather than what makes them necessary. A handler that reached either would be the instance-wide circle enumeration [canonical §7](../design/00-canonical-conventions.md#cross-circle-access-returns-404-never-403) exists to close |
| `TimerChangeIsPushed` | A write that moves a respawn window recomputes the derived state that depended on it, **in the same transaction** | `TestRouteRegistry_EveryTimerWritingRoute_PushesTheInvalidation` over the route registry, plus `TestSeedTimers_RecomputesEveryBoardTheWindowsMoved` for `tod-serve seed timers`, which is the one such write with NO route and is therefore invisible to that gate. `timer_change` is the one `change_reason` the append-only log cannot show, so it is pushed rather than derived. Since [ADR-0013](../adr/0013-the-timer-invalidation-joins-the-writing-transaction.md) the push takes the writing transaction's query set — including the reads, because `catalogue.ResolveTimer` on a pooled connection would recompute the board from the window that was there BEFORE — so a failed push rolls the window write back rather than merely failing the request. `catalogue.TimerInvalidator` refuses a nil port at the write, so an unwired instance cannot start one |
| `TimerPushIsTransactional` | A moved window and the boards derived from it commit together, or neither does. There is no instant at which the row has moved and the projection has not been told — including one a crash could land in | `TestRouteRegistry_EveryTimerWritingRoute_RollsBackWhenTheInvalidationFails` drives every flagged route with a failing invalidator and compares the database — the override, the catalogue timer, the derived state and the audit log — before and after: a route that kept what it wrote is a red build. `TestTimerWrite_TheInvalidation_RunsBeforeTheWriteIsVisible` covers the crash window itself over all FIVE derivation-moving write paths — the three routes, `tod-serve seed timers`, and the `is_quake_target` flip that moves no window at all — and asserts **both** halves — the change is visible on the transaction's own query set and not on a pooled one — because asserting only the first would pass for a push that ran after the commit and only the second for a push handed a query set that sees nothing. `TestPutCircleTimerOverride_WhenTheProjectionsOwnWriteIsRefused_TheOverrideIsNotThere` is the same rollback under a real constraint violation rather than a fake error, and `TestApplySeed_AFailurePartWay_KeepsTheWindowsItWroteAndNoOthers` pins the unit of atomicity for a seed at ONE window: a run that stops part-way leaves each written window with its boards recomputed and the rest untouched. `TestPutOverride_TheRecomputation_ReadsTheWindowItJustWrote` is the half none of those reach — it asserts the CONTENT of the cached row, because a push that joins the transaction and RESOLVES off the pool is atomic and wrong, and every gate above it passes for that. [ADR-0013](../adr/0013-the-timer-invalidation-joins-the-writing-transaction.md) |
| `QuakeFlagChangeIsPushed` | A flip of `raid_target.is_quake_target` recomputes the derived state of every live circle on the instance — every server, overridden circles included — **in the transaction that writes the flag** | `catalogue.Service.Update` takes the same `catalogue.TimerInvalidator` a window write does and calls `OnQuakeTargetChange`, which fans out over `ListLiveCircles` rather than one server's: `raid_target` is instance-wide and carries no server, and an override replaces a circle's WINDOW and never this flag, so the skip that is right for `OnCatalogueTimerChange` would be exactly wrong here. `TestQuakeTargetFlip_TheBoardAndTheDetailPage_Agree` is the behavioural gate — before it, the board said `overdue` with a ToD and `getTargetState` said `up` with none, indefinitely, because `boardEntry` reaches `up` only from a cached `up` and a stale `overdue` row can therefore never recover on read. `TestOnQuakeTargetChange_EveryLiveCircle_IsRecomputed_IncludingTheOverriddenOnes` is the fan-out over four circles on three servers, one of them overridden; the three properties ADR-0013 gave the window writes come from the same tables `TestTimerWrite_*` drive, which this write is the fifth entry in; `TestUpdate_OnlyAnActualFlagFlip_PushesTheInvalidation` closes the other direction. The reason recorded is `timer_change`, which is honest rather than approximate: `IsQuakeTarget` is a field of `consensus.Timer`, so the timer the derivation ran under is what changed. `TestOnQuakeTargetChange_ACircleWithNoReports_GetsNoCacheRow` holds the instance-wide fan-out to the cache's one predicate, so it cannot hand the nightly job a few hundred orphans to alert on. **The nightly verify job did catch this** — it recomputes and the recomputation wins — which is exactly why it must not be the mechanism: up to twenty-four hours of a confidently wrong board is this project's named failure mode, not its backstop. Issue #21 |
| `RenderReadsOneSnapshot` | A render reads **every derivation input from ONE state of the database** — the effective timer, the derived row, the quake log, and the target itself. There is no interleaving in which it shows the offsets from one timer beside a `died_at` clustered under another's ε, nor one in which it renders `target.is_quake_target` beside a status derived under the other value of that flag | `store.DB.InReadSnapshot`, a deferred, `query_only` transaction on a second pool, taken by `projection.Service.Board` — [ADR-0014](../adr/0014-a-deferred-read-pool-for-multi-read-renders.md). The gate is behavioural, because one that proved the primitive exists or that `Board` calls it would prove nothing about what a render shows: `TestBoard_ATimerCommittingMidRender_NeverPairsAWindowWithADiedAtFromAnotherTimer` rewrites the timer continuously on another goroutine while it renders, and requires every row to equal one of exactly two answers computed independently by `internal/consensus` — not by rendering the board, which would put one derivation on both sides of the assertion. It was watched failing against the pooled reads it replaces, within a few renders on every attempt. `TestGetTargetState_AQuakeFlagFlippingMidRead_NeverRendersItAgainstTheOtherDerivation` is the second half, and covers the input a target ROW carries: `is_quake_target` reaches `consensus.Timer` and decides whether the answer truncates at a quake, so `catalogue.List`/`Get` gained `ListIn`/`GetIn` — query-set-aware siblings — and the board and `getTargetState` read the target inside their snapshots. It fails within a few reads against a target read taken outside. The caller-facing spellings stay poolless because `internal/api` is the one layer that never names a query set ([ADR-0013](../adr/0013-the-timer-invalidation-joins-the-writing-transaction.md)). Underneath it, `TestInReadSnapshot_AWriteCommittedWhileItIsOpen_IsNotVisibleInsideIt`, `TestInReadSnapshot_HoldsNoWriteLock_SoAConcurrentWriteCommits` and `TestInReadSnapshot_AWriteThroughIt_IsRefused` hold the three properties of the primitive itself, each also watched failing |
| `VerifyDiffsOneSnapshot` | **Fixed but NOT gated, and recorded that way on purpose.** `projection.Service.Verify` reads the same pair — a cached row beside the timer a window write commits with it — and reads the circle's `min_reporters_to_supersede`, which is a derivation input, inside the snapshot rather than from the `ListLiveCircles` row that opened the sweep. So the same tear made a moving timer look like drift: an ERROR per field, on a healthy instance, from the job whose only value is that its alert means something. It takes the same snapshot and the tear is closed | Nothing behavioural, and no gate name is claimed for it. The interleaving is reachable — reproduced at two to seven sweeps in forty — but only by padding the circle with eighty reported targets so the sweep's vulnerable gap lined up with the write, a phase relationship tuned on one machine. A sampled test that misses one run in eight, costs seventy seconds under `-race` and may be vacuous on other hardware would be believed rather than trusted, which is worse than this row. What holds it up instead is `RenderReadsOneSnapshot`'s primitive gates plus review of one call site. `CacheIsNotAuthority` is unaffected: a real discrepancy is still found and still alerts. A circle tombstoned between the listing and its turn is now skipped rather than swept — a deleted circle must not come back onto the recompute path — and `CirclesChecked` counts up as circles are actually checked, so it never claims one the job deliberately passed over |
| `DerivedWriteFollowsItsSnapshot` | **A known gap, recorded rather than closed.** Every path that derives a state also WRITES it, and the write happens after the snapshot it derived from ends — the board's read-miss rebuild, `getTargetState`'s refresh, the verify job's repair. A timer committing in that gap leaves a cached row derived under a window that has since moved | Nothing. It is bounded, not closed: the next report for that target, the next timer change, or the nightly `verify-states` replaces the row, and `CacheIsNotAuthority` is what makes that recovery real. Closing it means the write joining the timer's own transaction, which is a read path taking the write lock — the trade [ADR-0014](../adr/0014-a-deferred-read-pool-for-multi-read-renders.md) declined. It is unchanged from before that ADR: the same two pooled reads were equally stale |

## Architectural laws

| Law | Enforced by | Lands in |
|---|---|---|
| HTTP routes are declared only in `internal/api` | `ROUTE001`, an AST analyser in `internal/repogate`, run by `TestROUTE001_Routes_AreDeclaredOnlyThroughTheRegistry`. It is stricter than the law: within that package exactly ONE file may call the framework's registration functions, and it takes an operation id from the registry rather than a method and a path — so a route that carries no permission, no scopes and no tenancy flag is unregisterable rather than merely discouraged | landed |
| A `database/sql` handle never leaves `internal/store` | `SQL001` greps every non-test file outside the package for the import, which answers *who imports it*; `SQL002` in `internal/repogate` is the AST analyser for the half a grep cannot see — whether a handle can be OBTAINED without importing it, which `db := store.Raw()` does. `TestSQL001_DatabaseSQLOutsideTheStore_IsReported` proves the grep fires; `TestSQL002_TheStore_HandsOutNoHandle` runs the analyser. Its one exemption, generated `internal/store/sqlitegen`, is earned by `TestSQL002_TheGeneratedExemption_IsNotAWayOut`. `store.ErrNoRows` is re-exported so that a caller distinguishing "no such row" from a real failure does not have to break the rule to spell the sentinel — a sentinel is an error value, not a handle | landed |
| `internal/consensus` is pure | See the consensus table above | landed |
| **Outbound HTTP has exactly one exception, and it addresses loopback** | The container image is `FROM scratch` — no shell, no curl — so the binary probes its own listener for the image's `HEALTHCHECK`. That request cannot go through the guarded client, because the guard's deny list covers loopback: a probe of our own listener is precisely what an SSRF guard exists to refuse. So `internal/probe` is allowed by name in `NET001`, and `PROBE001` is the other half — it holds that package to naming no host, and reading no configuration, so only the PORT comes from the listen address. `TestLivenessURL_IsAlwaysLoopback` is the authority: it drives the URL builder across every spelling of a listen address, including names that resolve elsewhere, and requires the result to address loopback every time | landed |
| Outbound HTTP originates only from `internal/identity`, to allowlisted hosts, through the one guarded client in `internal/identity/outbound` | `NET001` in two halves: `http.Client`, `http.Transport` and `net.Dial` outside `internal/identity/outbound`, and `http.NewRequest` outside `internal/identity`. Construction is confined to one package because a provider building its own client is a provider with no guard in the path. Plus `TestDenyReason_EveryDeniedAddress_IsRefused`, which drives the deny list directly — it must deny link-local and cloud-metadata addresses, not merely RFC1918 | landed |
| `web/src` contains no `fetch` or `XMLHttpRequest` outside `web/src/api` | `tod/no-network-outside-api`, a local ESLint rule in `web/eslint-rules`, **plus** `WEB001` in `scripts/repo-gates.sh`. Two mechanisms because they fail differently: a lint rule is switched off by an `eslint-disable` comment on the offending line and the grep is not, and the grep runs in the CI job that has no npm installed at all. The rule is the AUTHORITY — it resolves through the scope, so a property named `fetch` is not the global and a local binding that shadows it is not either, both of which a grep gets wrong in opposite directions — and its own unit test drives it with each shape it exists to catch. It also covers `XMLHttpRequest`, `sendBeacon`, `EventSource` and `WebSocket`: realtime lands in `web/src/api` with everything else, not in whichever component wanted it first | landed |
| **Every request the console makes is an operation the document publishes, reachable by any client with the right credential** | `TestAPIParity_EveryConsoleRequest_IsReachableWithAScopedToken`. **The two sides do not share a derivation**: one is the console's own source — every `api.<operationId>(` under `web/src`, read off disk — and the other is BEHAVIOUR, each of those operations driven over HTTP with a real personal access token scoped to exactly what the document declares. Anything but `403 session_required` proves a token reaches it; `session_required` is legitimate only where the registry already said no token does, and where the route names a permission that permission must be in `authz.CapabilityFloor()`. Any other 401 or 403 is a browser-only capability and a red build. `TestAPIParity_TheExtractor_ActuallyReadsTheConsole` is the empty-search-space guard | landed |
| The console never reads the browser's clock | `WEB002` in `scripts/repo-gates.sh`. `Date.now()` and a zero-argument `new Date()` are banned outright — there is nowhere in this console where a reading of the local wall clock is the right answer — and parsing an instant is confined to two files, each for a different reason: `web/src/lib/asof.ts` reads `performance.now()`, a monotonic counter of DURATION rather than of the wall clock, and `web/src/lib/format.ts` parses instants the SERVER sent. `src/lib/asof.test.ts` drives the arithmetic, including two browsers whose system clocks disagree rendering the same countdown. [Canonical §1](../design/00-canonical-conventions.md#1-time) | landed |
| **An answer belongs to the query it answered** | `internal`-style purity in the console: `web/src/app/resource.ts` is the whole of what decides what a screen sees, it depends on neither React nor the transport, and `resource.test.ts` drives it directly rather than through a renderer. A previous answer is carried forward only across another attempt at the SAME query — a reload or a poll tick — and never across a change of query. Without that split, a filter change whose first request failed relabelled the previous filter's rows as the new filter's: `loading` false, `error` null, and an officer filtering the board to a critical status during a transient failure would act on rows that do not match what they asked for. Both halves are covered — the failure path and the still-in-flight path — and each was proved by restoring the old carry and watching the named test fail | landed |
| **A refresh that failed is never invisible** | `Resource` carries `stale` and `staleError` on the BASE type, so no hook can drop them — they used to be polling-only, and a failed `reload()` on a plain resource kept the old rows and discarded the failure entirely. Every explicit reload in this console follows a WRITE, so that swallowed failure left somebody looking at the state from before their own retraction, revocation or role change and believing it was current. `error` and `staleError` are separate facts and neither implies the other: `error` means there is nothing to show, `staleError` means what IS shown is real but out of date. `WEB003` in `scripts/repo-gates.sh` closes the half a type cannot — a module calling `useResource(` or `usePoll(` renders `<StaleNotice`, or carries a counted `// stale:` waiver naming what renders it on its behalf. It fired on its own author's first run | landed |
| The console is served from `go:embed` and reaches no external host | `internal/ui` embeds `internal/ui/dist`, which `make build-web` stages from `web/dist`; `TestHandler_TheDocument_ReferencesNoExternalHost` asserts the shipped page names no `http://`, `https://` or protocol-relative host, because a strict deployment has no CDN and an asset that loads from somewhere else is an asset that does not load. `ui.Available()` and `ui.Handler()` agree (`TestHandler_Availability_AgreesWithTheHandler`), so a binary built without the console SAYS so at startup rather than serving a blank page that looks like a broken one | landed |
| **The console's security headers come from the binary, not from the proxy** | `TestConsole_ServesASelfContainedCSP` over every console path and both a GET and a POST — the document, a client-routed path, a hashed asset and a refusal, because headers attached only on the success path are missing exactly where an injected document would be most useful. It asserts the CSP by DIRECTIVE rather than as one string, so tightening it is not a red test while weakening it is: every source must be `'self'`, `'none'`, `data:` or `'unsafe-inline'`, and a policy naming a HOST fails. A header set on the reverse proxy protects the one deployment somebody remembered to configure; this one protects `docker run`, `compose.local.yaml` and a laptop too. **HSTS is deliberately NOT among them** — whether this origin is reachable over plain HTTP is a fact about the TLS terminator, and `deploy/compose.local.yaml`'s TLS front does not set it either, because pinning `localhost` to https for a year would break every other project on that machine | landed |
| An unknown path under `/api/v1` answers a problem, never the console's HTML | `TestConsole_AnUnknownAPIPath_IsAProblemAndNotThePage`. The split is derived from the route registry — `TestConsole_TheAPIPrefixSet_ComesFromTheRegistry` drives every unversioned route and requires the console not to have answered it — so an operational endpoint added later is covered without anybody editing `internal/api/console.go`. Falling through would answer `200` with a page of HTML, which is canonical §7's "never HTTP 200 with an error body" broken by a routing decision rather than by a handler | landed |
| **Every injected entropy source is `crypto/rand.Reader`** | `RAND001`, an AST analyser in `internal/repogate` run by `TestRAND001_ProductionWiring_UsesCryptoRandReader`. Every constructor that mints a secret takes its randomness and refuses a nil one rather than falling back to a default — which makes a weak source a *construction error* instead of a review habit, but only makes the choice deliberate **at the wiring site**. Nothing in the type system forces that site to say `rand.Reader`, so this does: the analyser requires every `Entropy` field and every named entropy sink (`auth.NewMinter`, `core.NewGenerator`) to be exactly the `Reader` of a `crypto/rand` import — not a variable holding it, not a wrapper returning it, not an `io.Reader` parameter. `TestWiring_IdentityService_IsGivenCryptoRandReader` asserts the same thing at run time by identity rather than by shape, and `TestRAND001_AWeakOrIndirectSource_IsReported` drives seven wrong wirings through the gate | landed |
| `time.Now` appears only in `internal/clock` | `CLOCK001`, an AST analyser in `internal/repogate` run by `TestCLOCK001_Repository_HasNoTimeNowOutsideClock`, so an aliased import does not defeat it. The grep in `scripts/repo-gates.sh` is a pre-check, not the authority | landed |
| Every typed id names a table that exists | `TestEntityMarkers_EveryName_IsATableInTheDomainModel` reads the markers out of `internal/core/id.go` with the Go parser and checks each against the [domain model](../design/01-domain-model.md) | landed |
| The applied schema holds exactly the tables the domain model lists | `TestSchema_TableSet_MatchesTheDomainModel`, compared in both directions, so a table added without a row in the document is as red as a documented table nobody created | landed |

## API invariants

| Invariant | Enforced by | Lands in |
|---|---|---|
| A deleted circle stops existing, and so does every credential in it | `circle.deleted_at` is a TOMBSTONE: `deleteCircle` cannot remove the row because `tod_report`, `quake_event`, `invite_redemption` and `audit_log` are append-only by trigger and reference it. Every read carries `deleted_at IS NULL`, the unique index on the name is PARTIAL so the name is released (`TestDelete_ReleasesTheName_SoItCanBeCreatedAgain`), an invite naming it answers the same `404 invite_invalid` an unissued code gets, and `internal/auth` refuses a credential bound to a membership in it on the very NEXT request — the same rule revocation uses. `TestDeleteCircle_EndsTheCircleAndEveryCredentialInIt` covers the member, the owner who deleted it, and a route that is not circle-scoped | landed |
| A principal of circle A gets `404` — never `403` — on every circle-scoped operation against circle B | `TestTenancy_CrossCircle_EveryOperationDenies`, derived from the **route registry** rather than a hand-written list, so a new uncovered route is a red test. **It is PARTIAL and logs exactly how partial:** 25 of 27 circle-scoped operations have handlers and are driven, and the other 2 — `subscribeCircleEvents` and `replayCircleEvents`, which have no handler at all — are enumerated in `uncoveredCircleRoutes` with the milestone that owns each — compared against the registry in *both* directions, so a route that gains a handler and stays listed is as red as one that loses coverage and is not. The middleware underneath it is enforced over routes with no handler at all: `TestTenancy_TheMiddleware_AnswersNotFoundAcrossCircles`, `TestTenancy_AMalformedCircleID_AnswersNotFoundToo` and `TestTenancy_IsDecidedBeforePermission` | 25 of 27 landed; the last 2 with Phase 6 (`internal/events`) |
| **First-run setup is open exactly while nobody administers this instance, and that is DERIVED** | `instancegrant.AdministratorExists`, asked through `authz.ExpandInstance` so it cannot decide differently from `auth.Principal.Holds`, **and** through `instancegrant.CanAuthenticate` so a grant nobody can carry does not count — an instance grant is on an identity, an identity reaches a request only through a membership, and the ledger outlives the membership. `tod-serve doctor` calls the same predicate, so an operator told nobody can administer this instance is never then told setup is over; `TestSetupRoutes_AnAdministratorWhoCannotLogIn_ReopensTheWindow` drives that state. There is no `setup_complete` row and no boolean anywhere: the window closes because an identity holds `instance.security.manage` — or `instance.owner`, which expands to it — and it REOPENS if every such grant is revoked, which is what makes an instance nobody can sign into recoverable from the browser as well as from the console. `TestSetupRoutes_AnAdministratorExists_EveryOperationRefuses` drives both keys, `TestSetupRoutes_AnAdministratorRevoked_ReopensTheWindow` drives the other direction, and `TestSetupState_AHalfFinishedSetup_IsReportedAndResumable` drives the state the whole design exists for: an `instance` row with no administrator behind it, which `/meta` calls `configured` and which must not close the door. Deriving it from the row instead is the lockout ([ADR-0016](../adr/0016-first-run-setup-is-an-env-token-and-a-derived-window.md)) | landed |
| **An unset `TOD_SETUP_TOKEN` and a wrong one are the same refusal** | `SetupConfig.authorises` runs `core.Secret.Equal` — `subtle.ConstantTimeCompare` — BEFORE it consults whether anything is configured, and both leave through one `404 not_found`. `TestSetupRoutes_WrongToken_IsTheSameRefusalAsUnset` compares the two responses field by field rather than by status, because a differing `detail` leaks exactly as well as a differing code, and it asserts the two tokens are the same LENGTH so the comparison is not short-circuited before it starts. `TestSetupConfigAuthorises_TheWholeTruthTable` covers the case with no interesting HTTP shape: an unset token and a caller presenting nothing are two empty strings, and an equality test alone would hand the instance to the first stranger who loaded the page | landed |
| **Every route authorised by `TOD_SETUP_TOKEN` is covered by all three refusals** | `api.SetupRoutes()` is the substrate — the same shape `CircleScopedRoutes()` gives the tenancy gate — and `TestSetupRoutes_TokenUnset_EveryOperationRefuses`, `TestSetupRoutes_WrongToken_IsTheSameRefusalAsUnset` and `TestSetupRoutes_AnAdministratorExists_EveryOperationRefuses` are each derived from it, so a second setup route cannot be added uncovered. Both halves are enforced in the route MIDDLEWARE rather than in a handler, so a route that carries the kind cannot be served with one of them missing. `TestAPIParity_EveryConsoleRequest_IsReachableWithAScopedToken` additionally drives them with the strongest possible PAT and requires the `404`: without that it would score a token-refused route as token-reachable, which is a pass over a request that never got in | landed |
| **First-run setup creates at most one circle, whatever races it** | The wizard reads the instance's circles and every write after that is a decision made from that read, so two runs could each see none and each create one. `circle.CreateFirst` re-counts and inserts inside ONE transaction, and the writing pool is `_txlock=immediate`, so the second caller cannot BEGIN until the first has committed — which holds across processes too. `TestCreateFirst_ConcurrentCallers_CreateExactlyOneCircle` races 16 callers over 8 rounds and is red when the count moves outside the transaction; `TestRunSetup_ConcurrentRuns_LeaveOneCircleAndTheWinnersInstance` asserts the outcome through the edge, and `TestRunSetup_ARepeatedRequest_MintsNoSecondOwnerCode` pins WHICH refusal a retry gets, because "that name is taken" reads as an instruction to pick another one. `setup.Service.Run` also admits one run at a time, so a losing run is refused before it writes its instance row and its provider — **that half has no gate**, because concurrent requests against this store serialise on their way in and a test cannot enter the window ([ADR-0016](../adr/0016-first-run-setup-is-an-env-token-and-a-derived-window.md)) | landed |
| **Redeeming an owner grant while nobody administers the instance grants `instance.owner`, in the join's own transaction** | `membership.Service.grantFirstAdministrator`, reading `instancegrant.AdministratorExists` and appending through `instancegrant.Service.DecideInTx` inside the transaction that created the membership — SQLite has one writer, so a second redemption sees the first rather than both passing a check neither had committed. It is an owner GRANT and never an invite: `invite` carries `CHECK (role <> 'owner')`, so a leaked invite cannot reach the branch at all. `TestEndToEnd_SetupWizardToConfiguringAnIdentityProvider` is the acceptance path — a fresh database to a browser session that registers an identity provider, with no `docker compose run` in it — and `TestEndToEnd_FreshDatabaseToConfiguringAnIdentityProvider` drives the second operator, for whom the branch is closed and the console is the only way in | landed |
| Every operation declares `Security` and `x-tod-permission` | `TestSpec_EveryOperation_DeclaresAnOperationIDSecurityAndPermission`, plus `TestSpec_Extensions_MatchTheRouteRegistry`, which compares every extension against the registry the middleware itself reads. `TestSpec_AFloorOperation_OffersOnlyTheSessionScheme` is the published half of the capability floor: a floor operation does not offer the bearer scheme at all | landed |
| Every operation has an explicit `operationId` in `lowerCamelCase`, never renamed | `TestRouteRegistry_EveryOperationID_IsLowerCamelCase` and the spec lint for presence; `SPEC001` in `scripts/spec-diff.sh` runs `oasdiff` against the base branch with `api-operation-id-removed` turned **on** — it is informational by default, and a check that is off by default is a check nobody has | landed |
| Every POST that creates domain state requires `Idempotency-Key` | `TestRoutes_EveryStateCreatingPost_RequiresIdempotencyKey`, over the route registry in both directions. The middleware replays on `(membership, key)`; `TestIdempotency_ARotatedToken_StillReplays` is the reason it is keyed on the membership and never on the token. Where no membership principal exists yet — `redeemInvite`, `authenticateIdentity`, `runSetup`, an instance-realm create — the header is still required and the handler owns the replay, because `idempotency_record.principal_membership_id` is `NOT NULL`. **That registry test is a DECLARATION check and drives no request**, which is how `POST /setup` shipped declaring `CreatesState: true` and being served with no key at all: the setup branch of `routeMiddleware` called the handler directly. Every branch now leaves through one tail, `dispatch`, so the requirement is read from the route rather than repeated per branch, and `TestSetupRoutes_NoIdempotencyKey_AreRefusedBeforeTheyWrite` drives the setup routes for real. The other state-creating routes are still covered by the declaration alone — driving them needs a credential each, which the tenancy gate's machinery has and this does not | landed |
| Errors are RFC 9457 with a `code` from a closed enum, and never HTTP 200 with an error body | Response-validation middleware run across the whole `internal/api` integration suite — every request every test makes, including the ones the framework answers before a handler runs. `TestCheckResponse_AContractViolation_IsReported` drives each rule it holds with a response that breaks it | landed |
| Every error code has a documentation page | `TestErrorCodes_EveryCode_HasADocumentationPage`, in both directions, plus `TestErrorCodes_Catalogue_MatchesTheAPIDesign` against the fenced blocks in the API design. `DOC001` in `scripts/docs-check.sh` is the copy that runs with no toolchain; it used to be anchored to the start of a line, which meant it checked the first code on each row of a three-column block and silently ignored the other two | landed |
| The capability floor cannot drift from the canonical conventions | `TestCapabilityFloor_MatchesCanonicalConventions` parses the fenced block in [canonical §6](../design/00-canonical-conventions.md#6-permissions-and-scopes--one-catalogue-generated) and compares element by element, both directions | landed |
| No PAT scope reaches a capability-floor permission — there is no `admin:*` scope and no all-powerful token | `TestScopes_NoScope_ReachesACapabilityFloorPermission`, over every scope held by the strongest role | landed |
| No PAT scope reaches an instance-realm permission, floored or not | `TestScopes_NoScopeGrants_AnInstanceRealmPermission` over the catalogue, and `TestRouteRegistry_EveryInstanceRealmRoute_IsSessionOnly` over the registry — the second checks the ROUTE's declared scopes and the first checks the catalogue behind them, which are different failures. `ops.read` is instance-realm and deliberately NOT in the capability floor, so the floor's own test does not cover it. An instance grant belongs to an IDENTITY and a token is bound to a membership ([ADR-0005](../adr/0005-pats-bound-to-memberships.md)), so a leaked token reaches none of them however the ledger reads | landed |
| An instance-realm permission is granted by the ledger and by no circle role | `TestPermissions_InstanceRealm_IsGrantedOnlyByTheLedger`, in both directions: no role matrix grants one, **and** every one of them is a value `instance_grant.permission` can hold. The second half is what closed the hole — a permission no role grants and no ledger can hold is a permission nobody can ever exercise, which is what the catalogue described until [ADR-0012](../adr/0012-instance-grants-are-a-capability-ledger.md) | landed |
| **Every permission in the catalogue is required by some route, consulted by some handler, or expands to ones that are** | `TestPermissions_EveryPermission_IsRequiredByARouteOrExpandsToOnesThatAre` in `test/repo`, over the WHOLE catalogue rather than the instance realm alone. **The three arms come from three places on purpose** — the route registry as data, handler source as an AST, and `authz.Implies` — because a comparison whose sides share a derivation passes for any input. It replaces a known-gap list that held `instance.owner` for as long as `instance.owner` granted nothing: the permission was grantable, hash-chained into an append-only ledger, printed by `tod-serve init` and named by the deployment runbook, and **no line of code consulted it**. The map that remains is two-sided — naming a reachable permission is as red as omitting an unreachable one — and it currently holds `token.revoke`, which this gate found. Mutations watched: an unreferenced permission, an expansion that is empty, one containing only the key itself, one landing on a key nothing checks, and a reachable key named as unreached | landed |
| `instance.owner` grants every instance-realm permission, and reaches no token at any scope | [ADR-0015](../adr/0015-instance-owner-implies-the-instance-realm.md) is why the implication ADR-0012 rejected is accepted for this one key: at the authorization boundary, never in storage, so the ledger still records one decision revoked in one act. `authz.Implies` is derived from `Realm` rather than listed, so an instance-realm key added to the catalogue is one an instance owner holds without anybody appending to anything, and the gate above is what stops it expanding to nothing again. `TestEffectiveForSession_InstanceOwner_ExpandsToTheWholeInstanceRealm` builds its want set from `authz.InstancePermissions()` rather than from `Implies`, so the expansion is compared against the catalogue instead of against itself. **The floor is the load-bearing half**: `EffectiveForToken` takes no instance set, and `TestPrincipal_APATCarryingEveryInstanceGrant_ReachesNoneOfThem` drives a Principal whose `InstanceGrants` are FULLY POPULATED — a state the authenticator never produces — so wiring the ledger into the token arm is red without needing a row. `Principal.Holds` refuses on the credential KIND rather than on the field being empty, because a `Holds` resting on that would answer `insufficient_scope` for a scope no token can carry. `TestEffectiveForSession_TheExpansion_NeverReachesAToken` asserts the session arm DOES reach them in the same loop, so neither side passes over an empty search space | landed |
| **An instance nobody can administer says so where an operator looks** | `checkAdministrable` in `cmd/tod-serve/doctor.go` reports a **PROBLEM** when no identity holds `instance.security.manage`, counting the expansion rather than the ledger's rows — a doctor reading raw rows would report a problem an `instance.owner` grant had already fixed, and the two disagreeing about one database is worse than either answer. **A grant alone is not an administrator**: an instance grant is on an IDENTITY and an identity only reaches a request through a membership, so the check requires at least one LIVE membership in a LIVE circle — `ListCirclesForIdentity`, the same predicate `listCircles` serves and the same one `Authenticator.membership` enforces per request, rather than a second definition of "can act". The ledger outlives a membership, so without that half an identity could hold `instance.owner` while every credential it could present was refused, and this report would have called the instance healthy. `TestDoctor_WhoCanAdministerTheInstance_IsAProblemWhenNobodyCan` drives nine report shapes and asserts the EXIT CODE, because a deploy is what reads it: nobody, `instance.owner`, the narrow key alone, `ops.read` only (which must NOT count), granted-then-revoked (the ledger is append-only, so the grant row is still in the listing), a revoked MEMBERSHIP, a deleted CIRCLE, one-revoked-one-live (so the filter cannot pass by refusing everybody), and two administrators. The two states have different sentences because they have different fixes — "nobody holds it" is answered by a grant, "they hold it and cannot log in" is not — and the dropped identities are COUNTED rather than passed over. The recovery path stays open — `tod-serve instance grant` holds the database and needs no credential — which is why this is a report rather than a refusal. `.github/workflows/deploy.yml` waives it BY NAME beside the other two on a first installation only, and requires all three to be PRESENT before waiving any | landed |
| **The runbook's first deploy produces a working administrator** | `deploy/smoke.sh` boots the shipped image and drives it: 403 on `/admin/identity-providers` before any grant, `instance grant --permission instance.owner` from the console, then 200 on the SAME session — and the backup it takes afterwards is required to still name an administrator. `TestEndToEnd_FreshDatabaseToConfiguringAnIdentityProvider` walks the same sequence in-process against a fresh database, granting the key the RUNBOOK names rather than the one the route declares, and asserts `doctor` reports the problem before the grant, none after it, and the problem again after the revocation. The 403-first half is what stops the 200 proving nothing | landed |
| Every code in the closed error enum is emitted by this binary, or named with the milestone that will emit it | `TestErrorCodes_EveryCode_IsEmittedOrExplicitlyPending` parses both code declarations with the Go parser and walks every non-test source file for a reference, comparing against `pendingCodes` in both directions. `DOC001` and `TestErrorCodes_EveryCode_HasADocumentationPage` compare the enum against the docs directory rather than against what is REACHABLE, which is how `report_immutable` sat in the enum with no possible emitter — no `PATCH` or `PUT` on a report exists, so the framework answers `405` and the `409` was vocabulary a client could branch on and never see. It is a source search, so it proves a constant is referenced rather than that the line can run | landed |
| **A role is granted only by somebody who holds it** | `refuseGrantAboveOwnRole` in `internal/membership`; `TestUpdate_AnOfficer_CannotGrantARoleAboveTheirOwn`. `member.manage` is held by officers, so without it an officer could set any member's role — including their own — to `owner` and acquire `circle.security.manage`, `circle.delete`, `token.mint` and `token.revoke`. Becoming an owner is deliberate everywhere else: `CHECK (role <> 'owner')` keeps it out of every invite so that a leaked bot token adds a revocable member rather than seizing the circle, and the only other door is a code the CLI prints once. Self-promotion walked past both. `TestUpdate_GrantingAtOrBelowYourOwnRole_StillWorks` and `TestUpdate_ADisplayNameChangeByAnOfficer_IsNotRefused` are the other direction — the guard fires only when a role actually moves, or renaming an owner would be refused to every officer | landed |
| Weak revocation's mitigation is an **owner's** to switch off | `requireSecurityKeyForInvalidation` in `internal/api`; `TestUpdateCircle_RevokeInvalidatesInvites_NeedsTheOwnerSecurityKey`, and `TestUpdateCircle_RevokeInvalidatesInvites_IsRefusedEvenWhenItChangesNothing` so that sending the current value is not a way to read it. `revoke_invalidates_invites` decides whether revoking a weakly-revocable member also kills the circle's live invites, so switching it off changes the circle's revocation guarantee — canonical §6's own test for a security key. It is the ONE per-field permission in this API: every other one is enumerable data on the route, and the registry-walking tests are blind to this by construction, which is why it has a named test instead | landed |
| A higher role never holds fewer permissions than a lower one | `TestRolePermissions_EachRole_ContainsTheRoleBelow` — a non-cumulative row would mean promoting somebody takes a capability away | landed |
| `docs/reference/permissions.md` is generated from the catalogue, never edited | `TestPermissionsDoc_Generated_MatchesTheCheckedInPage`; `-update` is refused when `CI=true` | landed |
| No token appears in a URL | `auth.RejectTokenInURL` refuses a credential-shaped query parameter under any name, and any parameter whose VALUE begins with the token scheme. `TestAuth_TokenInAQueryString_IsRejected` over the function and `TestAuth_TokenInAQueryString_IsRejectedOverHTTP` over the wire — including on a public route, where accepting one would teach clients the habit, and asserting the rejection does not echo the token it rejected. **It is refused at the top of `routeMiddleware`, before the auth-kind branches and before the invite rate limiter.** It lived inside `authorize`, which `AuthMetricsToken` and `AuthSetupToken` never call, so both were served with a token in the query — and `TOD_SETUP_TOKEN` in a proxy access log is an instance takeover. `TestRouteRegistry_EveryRoute_RefusesATokenInTheURL` drives every registered operation on both listeners, derived from the registry rather than a list so the next auth kind is covered the day it is added | landed |
| An invite code never reaches the server in a URL path or query | The link carries it in the **fragment** (`/join#TODI-…`), which no browser transmits; `previewInvite` and `redeemInvite` take it in a POST body. `TestRouteRegistry_NoOperation_TakesAnInviteCodeInTheURL` over the registry | landed |
| The Discord guild gate is evaluated on **both** `/join` and `/sessions` | `TestGuildGate_EvaluatedOnJoinAndSessions` over the two routes. The evaluator is one function, `identity.EvaluateGuildGate`, so the call sites cannot diverge — `TestGuildGate_EvaluatedAgainstTicketFacts_AtEitherEndpoint` covers it against the facts on the `credential_ticket`, never a cached copy and never a client-supplied claim. A gate on join alone would let `/sessions` mint a fresh PAT for someone who has left the guild. Both call sites reach it through one function in `internal/membership`, and `TestGuildGate_EvaluatedOnJoinAndSessions` drives every outcome through BOTH endpoints in one table | landed |
| A guild gate with no facts to evaluate **rejects**, never skips | `TestGuildGate_MissingRoleFacts_Refused`, and `TestEvaluateGate_EveryOutcome` for the three states `guild_roles_json` records — absent, `member: false`, and a member object. Reading an absent role list as an empty one would disable the gate for every user while appearing to enforce it, and collapsing absent into `member: false` would make `guild_membership_required` unreportable | landed |
| The authorization request asks for every scope the callback then uses, and no more | `TestAuthorizationURL_GuildGatedCircle_RequestsGuildsMembersRead`, in `internal/identity` and again over the scope set in `internal/identity/discord`. `guilds.members.read` grants `GET /users/@me/guilds/{guild.id}/member`; it does **not** grant `GET /users/@me/guilds`, which needs the separate `guilds` scope. The flow calls only the former, so it requests only the former — a call the scope set does not cover fails closed against members who are genuinely in the guild | landed |
| A scope the user declined is reported as such, never as a role failure | `TestGuildGate_DeclinedScope_ReportsScopeDeclinedNotRoleFailure`. `GET /oauth2/@me` returns the granted scopes, so the callback knows. `403 provider_scope_declined` and `403 guild_role_required` point at completely different fixes, and claiming the latter when we were never permitted to look is a confident mistake | landed |
| A Discord access token is accepted only if it was minted for **this instance's** application | `TestDiscord_ForeignApplicationToken_Refused`, plus `TestVerify_AudienceCheck_RunsBeforeAnyOtherCall` — an implementation that checked the audience second has already told an attacker's server the token is valid. Verification calls `GET /oauth2/@me` before anything else and rejects unless `application.id` equals the configured `client_id` — `401 credential_audience_mismatch`. This is what closes cross-instance replay; per-instance registration alone does not, because `GET /users/@me` honours any valid bearer token whichever application minted it | landed |
| A `provider_ticket` never appears in a URL query | It reaches the SPA in the redirect **fragment** (`<spa>/join#ticket=…`), which no browser transmits, and is cleared immediately. Failures use `#error=<code>` in the same fragment, so the redirect has one rule rather than one per outcome — `TestCompleteAuthorization_Failure_AlsoUsesTheFragment` is the half that would otherwise be forgotten. `TestNoTokenInURL_CallbackRedirectUsesFragment`. The SPA half landed with the console: the join page reads the fragment and clears it immediately, and `WEB001` plus the API-parity gate hold the browser to the same routes a token can reach | landed |
| **No public route resolves a caller-supplied `circle_id`** | `TestPublicRoutes_ResolveNoCircleFromCallerSuppliedId`, over the route registry — **landed**. A pre-authentication route that answers differently for a real circle than an unknown one confirms a circle's existence — including via which OAuth scopes the returned URL requests — which is what [canonical §7](../design/00-canonical-conventions.md#cross-circle-access-returns-404-never-403) hides. A circle comes from a **secret the caller was given** (an invite code) or from a **verified identity's own memberships**, never from an identifier they could guess | landed |
| Every public route accepting an invite code draws on **one shared rate-limit bucket** | `TestInviteOracle_PreviewAndAuthorizationURL_ShareOneBucket`, which exhausts the bucket through one route and asserts the other is exhausted too. `Route.InviteOracle` is what joins the bucket, so a third such route is a one-word decision rather than a second limiter somebody has to remember to wire; `TestInviteOracle_TheMeteredSet_IsEveryPublicRouteThatTakesACode` pins the set. The limit is applied **before** authentication and before the handler, which `TestInviteOracle_ARateLimitedCaller_ReachesNoHandler` proves — and that ordering is what makes the "a rejected probe stores nothing" property below true by composition | landed |
| An invite minted by a **PAT** is one use, at most 24 hours and a role no higher than `member`, and the response says so | `TestCreateInvite_ByAPAT_IsClampedAndTheResponseSaysCappedByPat` over the wire and `TestCreate_ByAPAT_EachAxisIsClampedIndependently` per axis, so a clamp that stopped working on one of them cannot hide behind the other two. `TestCreate_ByAPAT_ExactlyAtTheCap_IsNotReportedAsClamped` pins the boundary itself. This clamp is the ONLY reason `invite.create` is outside the capability floor while `token.mint` is inside it — canonical §6 — and the clamp is taken from the authenticated principal, never from the request body | landed |
| The first owner of a circle does not come from an invite | `CHECK (role <> 'owner')` on `invite`, asserted from the Go side by `TestGrant_AnOwnerInvite_IsUnrepresentableAtTheDatabase`. The one path that does make an owner is the single-use owner grant `tod-serve init` and `tod-serve circle create` print: `tod_meta` keyed by the code's hash, consumed by one compare-and-swap (`ConsumeMeta`), so `TestGrant_ASecondRedemption_IsRefused` holds even against two browsers redeeming one printed code | landed |
| `createAuthorizationURL` reveals no more about an invite code than `previewInvite` does | `TestCreateAuthorizationURL_AnUnissuedCode_RevealsNoMoreThanPreviewInvite` — the older endpoint's disclosure is the ceiling, so the newer one cannot be reasoned about separately and drift wider | landed |
| A rate-limited caller creates no `auth_flow` rows | **The invariant holds; it is the *assertion* that was missing.** `internal/api/ratelimit.go` runs the limiter as middleware, ahead of the handler that writes the row, and `TestInviteOracle_ARateLimitedCaller_ReachesNoHandler` proves a limited caller reaches no handler — so it writes no row, by composition. The bucket is per caller (`TestInviteOracle_TheBucket_IsPerCaller`) and refills on the injected clock. **This row previously named `TestAuthFlow_RateLimitedCaller_CreatesNoRows`, which has never existed**, as do four other places. That was a **coverage gap, never an open flood path**: the composition is real and tested at each step; nothing drove the end-to-end fact against a store | landed; the end-to-end assertion is not written |
| The circle recorded on an `auth_flow` is advisory; **redemption re-derives it** | `TestAuthFlow_CircleIsAdvisory_RedemptionReDerives`, which also asserts the ticket carries no circle at all. The flow resolves an invite's circle early so it can pick OAuth scopes and the guild to check, but `/join` re-reads the invite and decides. A 120-second-old snapshot must never outrank the live row — the same reason `target_state_cache` is not authority | landed |

## Test-integrity invariants

The ones that exist because the fastest route to a green build is to change the test.

| Invariant | Enforced by |
|---|---|
| The consensus golden corpus is not rewritten to go green | `test/golden/` is CODEOWNERS-protected — which covers the runner and its floor, not only the fixtures; `-update` is refused when `CI=true`; `TestGoldenCorpus_FixtureCount_NeverShrinks` asserts the fixture count is non-decreasing |
| The corpus exercises every derived enum value, rather than merely being large | `TestDerive_CorpusCovers_EveryStatusAndConfidence` — a corpus that never produced `overdue` would pass every day and pin nothing about it |
| Tests are not skipped or weakened to land a change | Review, plus a coverage floor per package |
| No test sleeps | `SLEEP001` in `internal/repogate`, run by `TestSLEEP001_Tests_DoNotSleep`; time-dependent tests use `testing/synctest` |
| No goroutine leaks in the event package | **Not enforceable yet, and not enforced.** `internal/events` does not exist — it is Phase 6 — and `goleak` is not a dependency of this module. The plan is `goleak.VerifyTestMain` in that package's `TestMain`; until the package lands this row guards an empty set, which is the shape `scripts/repo-gates.sh` refuses to print a green tick for |
| A gate's two sides do not share a derivation | Review, and the check is mechanical: **mutate one side and watch it fail.** A comparison in both directions proves nothing when both directions read from one source, and it looks *more* rigorous than a one-way check rather than less. The worked example is `Route.ConditionalRead`: the first gate over it compared the flag against the documented `304` in both directions and was green — while the document's `304` was generated **from** the flag, so both sides were one derivation wearing two hats. Setting the flag on a route that does not revalidate left them agreeing. The fix was to make the second side the BEHAVIOUR: `TestRouteRegistry_ConditionalRead_MatchesWhatTheRouteActuallyDoes` drives each route and requires the flag to predict what it answers. `scripts/repo-gates.sh` already refuses a gate that reports success over an empty search space; this is the same family with a full one |

## Operational invariants

| Invariant | Enforced by |
|---|---|
| The container health check never touches the database | `/healthz` does not, and `TestLiveness_MakesNoDatabaseCall` drives it against a **closed** store, so a single query added to it is a red test rather than a container killed mid-migration |
| `/metrics` is not exposed by default | Default `TOD_METRICS_ENABLED=false` — `TestMetrics_Disabled_HasNoHandlerAtAll`, which asserts there is no handler rather than an unreachable one. When enabled it binds a separate listener and requires a token compared in constant time: `TestMetrics_IsOnItsOwnListener_AndNotOnTheAPIRouter` also asserts `/metrics` 404s on the API router, and `TestMetrics_APAT_DoesNotOpenIt` is the never-gated-by-a-PAT-scope half |
| **A deployment snapshot is consistent against a running server** | `tod-serve backup` is `VACUUM INTO`, never a file copy: the database is in WAL mode, so the bytes at `tod.db` are not the database and copying them captures a torn read. `TestBackup_RestoresACompleteDatabase` compares the restored catalogue whole-value with `go-cmp` rather than by row count — a torn copy can have the right number of rows — and `TestBackup_WritesInFlight_StillProduceAConsistentCopy` takes one while a writer is running, then requires every row committed BEFORE the snapshot to be in it and every row it caught to hold the value committed with it. `deploy/smoke.sh` takes one against the running container on every CI build and checks it with `tod-serve doctor` and `tod-serve verify-states`. **The deploy opens its own snapshot too**, and that is not belt-and-braces: `tod-serve backup` pointed at the wrong volume — a fresh empty one, which is what a compose file naming a volume that does not exist gets — writes a valid 4 KB database and exits 0, reproduced against a real container. The deploy requires the copy's migrations to be current and its integrity check to pass, and ignores doctor's exit code, because a migrated-but-not-yet-`init`ed instance is a real state that has nothing to do with the backup | landed |
| **Every copy of this instance's origin names the same one** | The hostname is written down five times — the droplet's `.env`, the repository variable the deploy reads, `instance.public_url`, every enabled provider's `redirect_uri`, and the identity provider's own dashboard — and nothing compared any of them. `checkHostAgreement` in `cmd/tod-serve/doctor.go` compares the three this binary can see and reports a **PROBLEM**, not a warning: this instance serves the callback route, so a `redirect_uri` on another origin never arrives. `TestDoctor_APublicURLTheProvidersDisagreeWith_IsAProblem` asserts the **exit code** rather than the printed text, because a deploy is what reads it — and it drives the AGREEING case too, without which every other assertion would pass over a check that never fired. `deploy.yml` closes the pair this binary cannot see: it compares the droplet's `.env` against the repository variable before anything swaps, and runs `doctor` after `/healthz` and `/readyz`. Why it is a problem rather than a preference: `spaJoinURL` prefers `$TOD_PUBLIC_URL` over the stored row, so a wrong `public_url` still yields join links that look right, and the first symptom is an opaque OAuth error days later | landed |
| **NOT HELD: a real `deploy/.env` is not ignored by git** | **Nothing.** `deploy/env.example` line 1 says "Copy to `.env` NEXT TO the compose file. Never commit the real thing", and `.gitignore` carries no `.env` pattern at all — `git check-ignore deploy/.env` reports it is **not** ignored. That file holds `TOD_TOKEN_PEPPER`, `TOD_SESSION_KEY`, `TOD_SETUP_TOKEN` and the Discord `client_secret`, so the rule is real and only the mechanism is missing; the neighbouring `*.db` rule got its pattern for the same reason. The prose is a wish until `.gitignore` names it |
| **A published example secret cannot boot an instance** | `deploy/env.example` ships `CHANGE_ME_` placeholders and `serve` refuses any secret carrying that prefix, naming `openssl rand -base64 48`. `TestServe_PlaceholderSecret_Refused` drives the refusal; `TestEnvExample_EverySecretItShips_IsOneServeRefuses` reads THAT FILE off disk and requires each value in it to be one the binary rejects — so replacing a placeholder with something that merely looks like a secret is a red build. `TOD_TOKEN_PEPPER` is what makes a stolen database file useless on its own, so a copied-and-shipped pepper is the worst thing that file could produce | landed |
| **The shipped image is driven end to end, not merely built** | The `deploy / smoke` job runs `deploy/smoke.sh`, which IS the first-deploy walkthrough in [docs/operations/deployment.md](../operations/deployment.md) rather than a copy of it: migrate, init, seed, boot, redeem the one-time owner code over HTTP, use the token it returns, report a ToD, read the board, take a backup and check it. **Traefik is not in it** — there is no droplet in CI and ACME needs public DNS, so `deploy/compose.yaml` is only parsed. The runbook says so; an untested layer described as though it were covered is worse than one described honestly | landed |
| **A deploy that cannot have been bootstrapped is told apart from one that was broken** | The post-deploy `tod-serve doctor` collects every `PROBLEM` and then subtracts, BY NAME, only the two a genuinely new instance is allowed to have — no instance row, no enabled provider. The waiver is keyed on a **durable install marker** on the host, outside the data volume, written by the first deploy that brought the service up — never on the symptom, and never on the volume alone. `docker volume inspect` says only what is true NOW: a volume removed by `down -v` or lost to a rename gives a deployed host the state of a new one, and the deploy would then migrate an empty database over a real instance and go green. Marker present with no volume is **refused**. And the waiver is accepted only against the report a bootstrap actually produces — doctor's per-run lines must be there, and both waived problems must be PRESENT — because the `|| true` around the command turns a failed `exec` into an empty report, and an empty report has no problems in it. The symptom alone was not enough: an established database whose singleton was deleted reports the same missing row, the HTTP checks still pass without it, and branching on that string turned a broken production instance into a green deploy while discarding every other finding in the report | Driven over eight real report shapes, including an empty one, a failed `exec` and a half-bootstrap: a fresh instance passes, **the same report on a later deploy fails**, a fresh instance with a corrupt database fails, an established healthy one passes, and an established one with a public-URL disagreement fails | landed |
| **A container restart never migrates** | `serve` does not call `Migrate`; it calls `Ready` and refuses to start when the database is behind, which `/readyz` reports by name. The deploy workflow runs `docker compose run --rm tod-serve migrate` as its own step, after the `production` environment's required reviewer approved it. There is deliberately no one-shot migrate service with `service_completed_successfully` in `deploy/compose.yaml`: that would run on every `up`, including an unattended restart, which is the exact failure the rule exists for | landed |
| No secret is ever logged | `core.Secret` renders as `***` in `String`, `GoString`, `MarshalJSON`, `LogValue` and every `fmt` verb; `TestSecret_NeverRendered` and `TestSecret_NestedInAStruct_NeverRendered` marshal and log a whole config and assert no known secret value appears. The one hole — `fmt` reflecting over an **unexported** field — is pinned by `TestSecret_InAnUnexportedField_IsNotProtectedByFmt` rather than left to be rediscovered |
| **No secret is compared by value** | `SECRET001` — `TestSECRET001_NoSecret_IsComparedByValue`, an AST walk over `internal` and `cmd` banning a `Reveal()` on either side of `==` or `!=`. Every bearer credential checked at the edge goes through `core.Secret.Equal`, which is `subtle.ConstantTimeCompare`, and the two that need it most have no rate limit in front of them: the metrics listener has no bucket and first-run setup has no principal to key one on. It is a gate rather than a habit because the failure is invisible every other way — `x.Reveal() == y` passes the unit tests, answers the same status codes, and reads like a comparison. It was written deliberately during this gate's mutation run and nothing else noticed |
| A Discord access token is never persisted | Read and discarded inside the OAuth callback; only the derived subject, display name, guild ids and role ids reach `credential_ticket`. `TestDiscord_AccessToken_NeverPersisted` asserts no store call receives it — over a **recording** fake, which is why `internal/identity` takes port interfaces rather than a query set: without that, "store call" is not something a test can name |
| **NOT HELD: expired `auth_flow`, `credential_ticket` and `idempotency_record` rows are never swept** | **Nothing.** `DeleteExpiredAuthFlows`, `DeleteExpiredCredentialTickets` and `DeleteExpiredIdempotencyRecords` are generated by sqlc and **called from nowhere** — `grep -rn DeleteExpired --include='*.go' . | grep -v /sqlitegen/` returns nothing. `db/schema.hcl` carries `ix_auth_flow_expires_at` and `ix_credential_ticket_expires_at`, built for a sweep that no job, command or handler runs, and its comment says an unredeemed flow "is litter, not history". The rate limiter caps how fast these rows arrive, not how many accumulate; every one is unreadable after `expires_at`, so this is unbounded growth rather than a disclosure. It is written down here, ungated, rather than left implied by a query that exists |
| `identity_provider.client_secret` is never serialised or logged | It is a `core.Secret` — `***` on every rendering path above — and `listIdentityProviders` returns `client_id` but never the secret. Covered by the no-secret-is-ever-logged test |

## Repository gates

The ones that are about this repository rather than about the product. Each is in
`scripts/repo-gates.sh`, `scripts/docs-check.sh` or a script they call, and each runs in
`make check`.

| Gate | Refuses | Watched failing by |
|---|---|---|
| `PIN001` | A GitHub Action `uses:` that is not pinned to a 40-character SHA. It checks the SHAPE of a pin, not that the digest matches its trailing comment: a bare tag is the failure this catches, and a lying comment is a review problem | Review — it is a one-line grep with no state |
| `ACT001` | A `${{ … }}` expression interpolated into a workflow's shell script. GitHub substitutes the VALUE into the script text, so it is EXECUTED rather than read; the fix is always `env:` plus `"$VAR"`, so the gate has no exceptions — an exception list is a thing somebody appends to at 2am | `TestACT001_FiresOnEveryWayAnExpressionCanReachAScript`, over all six spellings GitHub accepts, and `TestACT001_PassesWhatItShould`, because a gate with false positives gets switched off |
| `ACT002` | A workflow shell script that does not parse under `bash -n`. Workflow shell is not compiled, not linted and not executed until a tag is pushed or a deploy is approved, so a broken script ships and is found at the worst possible moment. ANY diagnostic counts, not only a non-zero exit: for an unterminated heredoc, bash 5 exits 0 and merely warns | `TestACT002_FiresOnAScriptThatDoesNotParse` and `TestACT002_FoldsWhatYAMLFolds`, which is the false-positive half — a folded block and a plain scalar join their lines with a space, and extracting them separately fails a workflow Actions runs happily. `TestACT002_FiresWhenNothingWasExtracted` is the vacancy check |
| `ACT003` | A `docker compose run` or `exec` in a workflow that does not redirect stdin. A workflow script routinely reaches a remote shell ON STDIN (`ssh … bash -s <<'REMOTE'`) and both of those subcommands attach stdin by default, so the first one swallows the rest of the script: bash reads EOF, exits **0**, and the step reports success having silently skipped everything after it. `-T` does not fix it — that disables the TTY, not the stdin attachment — and `2>/dev/null` is not a stdin redirect, which is why the gate looks for a `<`. **This one is not hypothetical: it shipped.** On 2026-08-25 the first production deploy migrated the database, never ran `up -d`, went green, and the verification afterwards spent thirty attempts reporting the symptom | `TestACT003_FiresOnACommandThatEatsTheScript`, including the exact line that shipped, and `TestACT003_PassesWhatItShould` for the false-positive half — prose containing "re-run", a comment naming the command, and `2>/dev/null`, which must still be a finding |
| `ENV001` | A `TOD_*` the binary reads and `deploy/env.example` does not document, or a `TOD_*` named in `deploy/` that is not a constant in `cmd/tod-serve/root.go`. The second direction is the one that catches a typo: `TOD_TOKEN_PEPER` in a compose file interpolates to empty and the server then refuses to start naming a variable that IS set. **The two sides are independent hand-written lists** — a Go const block and a set of deployment files — which is what stops this being a both-directions check that reads from one derivation. The `TOD_DEPLOY_` prefix means "docker compose reads this and the binary never does", and the gate needs no allowlist because of it | `TestENV001_FiresWhenTheTwoListsDisagree` and `TestENV001_PassesWhatItShould`, over fixtures |
| `IMG001` | An image reference that is not pinned to `@sha256:`, or a digest with no readable tag inline or in a trailing comment. `PIN001`'s reasoning applied to images: a tag is mutable, so `node:24` in six months is not the `node:24` this was tested against, and a digest nobody can read back to a tag is a pin nobody can update. One interpolation is waived BY NAME — `${TOD_DEPLOY_IMAGE…}`, the image this repository builds, which the deploy workflow pins to an exact tag in the host's `.env` — and the waiver is counted and printed | `TestIMG001_FiresOnAnUnpinnedImage`, including the shapes that walk past a naive gate: a bare tag behind `--platform`, a digest with no tag, and `${PROXY_IMAGE:-caddy:2}` |
| `LBL001` | A Traefik label that NAMES a router, service or middleware that no label DEFINES. Traefik resolves these by string, so a router whose `service=` says `todserve` when the service labels say `tod-serve` is not an error anybody sees: the router exists, its target does not, and the host answers **404** — the same 404 Traefik gives a host it has never heard of, and the same one [the runbook](../operations/deployment.md) teaches an operator to read as "the container is not up yet". `docker compose config` does not inspect label contents at all. Both label spellings are read, `key: value` and `- "key=value"`, because the reference stacks on the target droplet are written the second way | `TestLBL001_FiresOnANameNothingDefines` over a bad service, one bad name inside a comma-separated middleware chain, and the list spelling; `TestLBL001_PassesWhatItShould` drives the **verbatim labels of a service running in production on that droplet**, so a gate that rejected a working configuration is a red test |
| `PROBE001` | `internal/probe` naming a host, or reading configuration. See the architectural table above | `TestLivenessURL_IsAlwaysLoopback` is the authority; the gate is the grep that runs without a toolchain |
| `ADR000`, `ADR001`, `ADR002`, `ADR003` | An ADR that states no downside, no reversal cost, or no considered options — and, first, a repository with no ADRs at all, which is what makes the other three something other than a green tick over an empty set. `ADR001`'s 1000-word ceiling is a proxy for the rule that matters: over budget is usually two decisions in one file. The format's whole value is in the parts people skip when rushed, so a template nobody enforces becomes a heading nobody fills in | Review; they have fired on real ADRs in this repository, which is how the word ceiling acquired its current value |
| `DOC002` | A gate defined in `scripts/` or in `test/repo` and not recorded on this page. The reverse drift from `DOC001`'s: a rule nobody knows is enforced is one somebody "simplifies" away in a refactor with no reviewer objecting | Its own vacancy check — it fails when it parses no gate names at all |
| `verify-commands` | A `make` target or a `tod-serve` verb named in `AGENTS.md` or `docs/operations/*.md` that does not exist. Verbs are resolved by ASKING THE BINARY (`--help` exits non-zero on an unknown command), so nested ones like `seed targets` are covered — a check over the top level alone would pass anything. Only backticked commands and lines inside fenced blocks are read, because matching bare prose reports `make sure` as a missing target | Review; the extraction is fence-aware precisely because the first version reported a sentence beginning "tod-serve is one more service" as a missing verb |

## Licence invariants

| Invariant | Enforced by |
|---|---|
| No timer data is bundled | Respawn and variance numbers load from the separate, optional `tod-serve-p99-seed` repository through `tod-serve seed timers --file`. `SEED001` in `scripts/repo-gates.sh` is the grep firewall, matching the one DKP puts around `dkp-p99-seed`; `TestEmbedded_NoTargetCarriesATimer` closes the other route in by asserting `catalogue.EmbeddedTarget` has no window field to fill |
| Target identity ships and is ours | Names, zones, expansions and categories are literals in `internal/catalogue/embedded.go`. `TestEmbedded_EveryTarget_IsValid`, `TestEmbedded_EveryNameAndAlias_IsUniqueAcrossTheCatalogue` and `TestEmbedded_EveryName_ResolvesToItself` check the list against the enum catalogue, both unique indexes and the resolve ladder. **What no mechanism checks is whether a zone or category is factually right**; the list is deliberately short for that reason |
| An unseeded instance is degraded, not broken | `TestResolveTimer_AnUnseededInstance_IsUnknownEverywhere` over the whole shipped catalogue, and `TestUnseededInstance_DerivesNoTimerAndStillCountsEveryReport`, which drives the real `consensus.Derive`: `status: no_timer`, and the time of death, the evidence and quake truncation all intact |
| A bad timer seed is never partly applied | The whole file parses and every target resolves before the first write — `TestApplySeed_ARowThatDoesNotResolve_LeavesTheCatalogueUntouched` and `TestSeedTimers_ABadFile_FailsAndLeavesTheCatalogueUnchanged` both fail a file on its last row and assert nothing was written. A file that is VALID and fails part-way through writing is a different case and is deliberately not all-or-nothing: since [ADR-0013](../adr/0013-the-timer-invalidation-joins-the-writing-transaction.md) each window is written and recomputed in a transaction of its own, because a whole-file transaction would hold SQLite's single write lock across every recomputation and block every report on the instance. `TestApplySeed_AFailurePartWay_KeepsTheWindowsItWroteAndNoOthers` is the gate on what that leaves behind, and the command prints both counts with their denominators on the failure path so an operator is never told a bare number that reads as success. The two failures carry OPPOSITE remedies — a rejected file must not be re-run, a half-written one must — so `ApplySeed` marks every pre-write refusal with `catalogue.ErrSeedRejected` and the command branches on it rather than printing counts a rejected run does not have. `TestApplySeed_EveryPreWriteFailure_IsMarkedRejected` drives each refusal and, in the same table, a mid-write failure that must NOT carry it |
| **NOT HELD: no copyleft or source-available runtime dependency** | **Nothing.** This row named `scripts/licence-gate.sh`, "classifying every module in `go list -deps ./...` and failing closed" — **that script does not exist**, no `make` target invokes it and no CI job runs one. Nothing in this repository classifies a dependency's licence. The rule is a real constraint on a project shipping an `Apache-2.0` binary; only the gate was imagined. Writing the script is the fix, and until then this row is the honest version |
| No game assets are bundled | Reviewed at release |

## What to do when two rules conflict

The invariant wins, and the conflict is a bug worth reporting. Say so rather than picking one
silently. [Canonical conventions](../design/00-canonical-conventions.md) is the tie-breaker between
any two documents; if a document disagrees with it, the document is stale.
